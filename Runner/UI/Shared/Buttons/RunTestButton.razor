@inject PresentationService.API2.PresentationService.PresentationServiceClient PresentationService;
@inject IMessageService MessageService;

<InlineButton @onclick="() => goToAsync()">Run</InlineButton>

@code {
    [Parameter]
    public string TestNameFilter { get; set; }

    async Task goToAsync()
    {
        var response = await PresentationService.BeginTestAsync(new BeginTestRequest(TestNameFilter));

        MessageService.AddMessage(response.Status.Code.ToString());
    }
}